import { HandleColorDirective } from './handle-color.directive';

describe('HandleColorDirective', () => {
  it('should create an instance', () => {
    const directive = new HandleColorDirective();
    expect(directive).toBeTruthy();
  });
});

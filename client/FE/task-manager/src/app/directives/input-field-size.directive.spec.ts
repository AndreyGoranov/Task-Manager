import { InputFieldSizeDirective } from './input-field-size.directive';

describe('InputFieldSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFieldSizeDirective();
    expect(directive).toBeTruthy();
  });
});
